
INCLUDE_DIRECTORIES(
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_BINARY_DIR}
)

SET(MOC_SRCS "")
SET(UI_HDRS "")

SET(
	SRCS
	DRobotDataLogger.cpp
)

MESSAGE(STATUS "SRCS: " ${SRCS})
MESSAGE(STATUS "UI: " ${UI_HDRS})
MESSAGE(STATUS "MOC: " ${MOC_SRCS})
